diff --git a/rvd/rvd_main.c b/rvd/rvd_main.c
index 8e01b35..c0bc050 100644
--- a/rvd/rvd_main.c
+++ b/rvd/rvd_main.c
@@ -43,6 +43,7 @@ int main(int argc, char **argv)
 	if (ret != 0)
 		return ret < 0 ? 1 : 0;
 	rss_hal_set_log_func(hal_log_bridge);
+	fprintf(stderr,"RVDT:main-after-daemoninit\n");fflush(stderr);
 
 	/* Initialize state */
 	rvd_state_t st = {0};
@@ -56,6 +57,7 @@ int main(int argc, char **argv)
 		RSS_WARN("control socket failed (non-fatal)");
 
 	/* Set up video pipeline */
+	fprintf(stderr,"RVDT:main-before-pipeline\n");fflush(stderr);
 	ret = rvd_pipeline_init(&st);
 	if (ret != RSS_OK) {
 		RSS_FATAL("pipeline init failed: %d", ret);
diff --git a/rvd/rvd_pipeline.c b/rvd/rvd_pipeline.c
index 6aafd11..12fff5a 100644
--- a/rvd/rvd_pipeline.c
+++ b/rvd/rvd_pipeline.c
@@ -327,14 +327,17 @@ int rvd_pipeline_init(rvd_state_t *st)
 		st->enc_shm_fd[i] = -1;
 
 	/* ── 1. Create HAL ── */
+	fprintf(stderr,"RVDT:pipe-entry\n");fflush(stderr);
 	st->hal_ctx = rss_hal_create();
 	if (!st->hal_ctx) {
 		RSS_FATAL("rss_hal_create failed");
 		return RSS_ERR;
 	}
+	fprintf(stderr,"RVDT:after-hal-create\n");fflush(stderr);
 	st->ops = rss_hal_get_ops(st->hal_ctx);
 
 	/* ── 2. Sensor config ── */
+	fprintf(stderr,"RVDT:before-sensor-cfg\n");fflush(stderr);
 	rss_multi_sensor_config_t multi_cfg = {0};
 
 	/* Detect multi-sensor: if [sensor1] has a name, use [sensor0]+[sensor1]+... */
@@ -383,6 +386,7 @@ int rvd_pipeline_init(rvd_state_t *st)
 		st->hal_ctx = NULL;
 		return RSS_ERR;
 	}
+	fprintf(stderr,"RVDT:after-sensor-load\n");fflush(stderr);
 	st->sensor_count = multi_cfg.sensor_count;
 
 	/* Validate primary sensor */
@@ -400,6 +404,7 @@ int rvd_pipeline_init(rvd_state_t *st)
 		return RSS_ERR;
 	}
 
+	fprintf(stderr,"RVDT:after-validation\n");fflush(stderr);
 	for (int s = 0; s < multi_cfg.sensor_count; s++) {
 		RSS_DEBUG("sensor%d: %s i2c=0x%02x bus=%d id=%d", s, multi_cfg.sensors[s].name,
 			  multi_cfg.sensors[s].i2c_addr, multi_cfg.sensors[s].i2c_adapter,
